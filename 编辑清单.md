# 编辑清单

## 2026-07-25 - 补光控制协议解码 RED/GREEN

- **trace_id**：`fill-light-20260725`
- **轮次/角色**：Round 1 / Builder
- **修改文件**：`User/App/binary_protocol_service_host_test.c`、`User/App/binary_protocol_service.h`、`User/App/binary_protocol_service.c`
- **操作类型**：修改
- **操作内容**：增加 `FILL_LIGHT_CONTROL 0x23` 的 4 字节负载、开关动作枚举、解码函数和合法/非法 action 主机测试。
- **关联模块**：MP157-F4 二进制协议、补光灯舵机控制
- **验证标准**：`gcc -std=c99 -Wall -Wextra -DBINARY_PROTOCOL_HOST_TEST -I User\App User\App\binary_protocol_service_host_test.c User\App\binary_protocol_service.c -o tmp\binary_protocol_service_host_test.exe; .\tmp\binary_protocol_service_host_test.exe`
- **验证结果**：通过，输出 `binary protocol host tests passed`
- **Git提交**：无，本轮尚未请求独立提交
- **远端同步**：未 push
- **回档记录**：无

## 2026-07-25 - 补光舵机服务与 RTOS 接线

- **trace_id**：`fill-light-20260725`
- **轮次/角色**：Round 2-3 / Builder
- **修改文件**：`User/App/fill_light_service.c/.h`、`User/App/fill_light_service_host_test.c`、`Core/Src/freertos.c`、`MDK-ARM/bisai_f407_project.uvprojx`
- **操作内容**：PB6/AF2/TIM4_CH1 输出 50 Hz PWM，0~270 度映射 500~2500 us，保持 2000 ms 后停止，并由独立任务发送 0x16 完成事件。
- **验证标准**：角度主机测试、Keil XML 解析和 F4 静态契约通过。
- **验证结果**：主机角度测试此前已通过；最终验证轮次重新执行。
- **Git提交**：无，本任务尚未请求提交 F4 工程。
- **远端同步**：未 push。

## 2026-07-25 - Round 6 最终静态验证

- **trace_id**：`fill-light-20260725`
- **轮次/角色**：Round 6 / Verifier
- **验证命令**：Qt `test_qt_kms_overlay_assets.sh`；F4 协议与角度 GCC 主机测试；Keil XML 入组检查；PB6/TIM4 静态契约；双仓库 `git diff --check`。
- **验证结果**：Qt 输出 `PASS: Qt KMS overlay assets contract`；F4 输出 `binary protocol host tests passed`、`fill light service host tests passed`、`Keil fill light source contract passed`、`F4 fill light static contract passed`。
- **MP157 部署**：VM 交叉编译产物为 ARM 32-bit EABI5，SHA256 `4b2d291fd128f7e69b1f1925991bc377a87c43e793b8a988edcafe3e89d16b70`；板端替换后哈希一致，Qt 与 overlay 进程已重启。
- **F4 边界**：未运行 Keil 固件编译，未下载到 F4，未做 PB6 示波器和舵机实测；代码已改和 Git 提交不等于 F4 板端生效。
- **Git提交**：等待本轮 F4 提交后回填。
- **远端同步**：等待本轮 F4 push 后回填。

## 2026-07-25 - MP157 补光状态机与跨层文档

- **trace_id**：`fill-light-20260725`
- **轮次/角色**：Round 4-5 / Builder
- **修改文件**：Windows 仓库 `main.cpp`、`qml/Main.qml`、`test_qt_kms_overlay_assets.sh`、协议/路线/模块 README；F4 `binary_protocol_service.md`、`fill_light_service.md`。
- **操作内容**：严格等待 0x23 ACK 和 0x16 完成事件，开灯完成后等待 5 秒再检测，检测成功/失败后先关灯，再回升 Z 轴；暂停/停止取消计时并在清 cycle 前关灯。
- **验证标准**：Qt 静态契约输出 `PASS: Qt KMS overlay assets contract`。
- **验证结果**：已通过；交叉编译和板端验证仍待执行。
- **Git提交**：无，本轮尚未请求提交。
- **远端同步**：未 push。
