# 硬件资源表

## 芯片与开发环境

- **芯片型号**：STM32F407ZGT6
- **内核**：Arm Cortex-M4F
- **系统时钟**：100 MHz
- **开发框架**：STM32 HAL + FreeRTOS + CMSIS-RTOS2
- **IDE**：Keil MDK，工程文件为 `MDK-ARM/bisai_f407_project.uvprojx`
- **当前任务**：模型检测前补光灯舵机控制
- **trace_id**：`fill-light-20260725`
- **最后更新**：2026-07-25

## 引脚分配表

| 引脚 | 功能 | 外设 | 复用 | 状态 | 冲突检测 | 备注 |
|---|---|---|---|---|---|---|
| PA9 | MP157 主链路发送 | USART1_TX | AF7 | 已使用 | 无 | 115200 8N1 |
| PA10 | MP157 主链路接收 | USART1_RX | AF7 | 已使用 | 无 | DMA2 Stream2 接收 |
| PC10 | 传送带步进电机发送 | UART4_TX | AF8 | 已使用 | 无 | EMM42 电机 ID 1 |
| PC11 | 传送带步进电机接收 | UART4_RX | AF8 | 已使用 | 无 | DMA1 Stream2 接收 |
| PD8 | ESP32S3 机械臂发送 | USART3_TX | AF7 | 已使用 | 无 | F4-ESP32S3 二进制链路 |
| PD9 | ESP32S3 机械臂接收 | USART3_RX | AF7 | 已使用 | 无 | DMA1 Stream1 接收 |
| PC6 | 摄像头步进电机发送 | USART6_TX | AF8 | 已使用 | 无 | 摄像头左右/上下电机 |
| PC7 | 摄像头步进电机接收 | USART6_RX | AF8 | 已使用 | 无 | DMA2 Stream1 接收 |
| PB6 | 补光灯舵机 PWM | TIM4_CH1 | AF2 | 本次新增 | 条件可用 | 最小系统板同时引出为 DCMI_D5；仅在 F4 未使用 OLED/CAMERA 接口时可作 PWM，当前 `.ioc` 未启用 DCMI |
| PA13 | SWD 调试数据 | SYS_JTMS-SWDIO | 系统 | 已使用 | 无 | 不得改作普通 GPIO |
| PA14 | SWD 调试时钟 | SYS_JTCK-SWCLK | 系统 | 已使用 | 无 | 不得改作普通 GPIO |

## DMA 通道分配

| DMA 资源 | 分配外设 | 传输方向 | 备注 |
|---|---|---|---|
| DMA2 Stream2 | USART1_RX | 外设到内存 | MP157 主链路 DMA + IDLE 接收 |
| DMA1 Stream2 | UART4_RX | 外设到内存 | 传送带 EMM42 回包 |
| DMA1 Stream1 | USART3_RX（CubeMX 保留） | 外设到内存 | 当前机械臂服务运行时使用 9600 波特率单字节 RX 中断，未启动该 DMA；重新生成工程时需保持文档与接收策略一致 |
| DMA2 Stream1 | USART6_RX | 外设到内存 | 摄像头电机回包 |
| 无 | TIM4_CH1 | 定时器比较输出 | 舵机 PWM 不使用 DMA，也不使用 TIM4 中断 |

## 时钟配置

| 时钟 | 频率/参数 | 用途 |
|---|---|---|
| SYSCLK | 100 MHz | F407 系统主频 |
| APB1 | 25 MHz | APB1 外设总线 |
| APB1 定时器时钟 | 50 MHz | TIM4 输入时钟；APB1 分频不为 1 时定时器时钟为总线时钟的 2 倍 |
| TIM4 预分频器 | `PSC=49` | 50 MHz / (49 + 1) = 1 MHz，即 1 个计数为 1 us |
| TIM4 自动重装 | `ARR=19999` | 20000 us 周期，即 50 Hz 标准舵机周期 |
| TIM4_CH1 比较值 | 500~2500 | 默认把 0~270 度线性映射为 500~2500 us 高电平 |

## 补光舵机硬件约束

- PB6 只输出舵机控制信号；舵机电源必须使用满足堵转电流的外部电源，不能由 MCU GPIO 供电。
- 外部舵机电源地必须与 STM32F407 GND 共地，否则 PWM 电平没有可靠参考。
- 默认脉宽范围 `500~2500 us` 仅作为首版可标定参数；首次上电应先断开机械负载或限制行程，用示波器确认脉宽后再连接开关机构。
- 舵机到达目标位置后，软件停止 TIM4_CH1 PWM；机械结构必须能在停止 PWM 后保持补光灯的开/关状态。
- `bisai_f407_project.ioc` 当前没有配置 PB6/TIM4，本模块在运行时由 `fill_light_service.c` 手动初始化；以后使用 CubeMX 重新生成工程时必须复查该手动资源没有被其它外设占用。
- 板级《M144Z-M4最小系统板IO引脚分配表——STM32F407版.xlsx》标注 PB6 同时连接 OLED/CAMERA 接口的 DCMI_D5。接舵机前必须确认该接口未使用，且不能把摄像头 D5 信号与舵机控制线同时接入。

## 变更记录

| 日期 | 变更内容 | 原因 |
|---|---|---|
| 2026-07-25 | 新增 PB6/AF2/TIM4_CH1、50 Hz PWM 和舵机电源约束 | 为模型检测前补光灯机械开关提供硬件控制资源 |
