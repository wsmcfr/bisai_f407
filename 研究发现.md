# 研究发现

## 2026-07-25 模型检测前补光灯与舵机控制

- **关联轮次**：`fill-light-20260725` / Round 1 / Research
- **本地检索范围**：`bisai_f407_project.ioc`、`MDK-ARM/bisai_f407_project.uvprojx`、`Core/`、`User/` 及工程根目录网表文件。
- **已确认平台**：芯片为 `STM32F407ZGT6`，工程使用 STM32 HAL、FreeRTOS 和 CMSIS-RTOS2。
- **已确认协议基础**：`User/App/binary_protocol_service.c/.h` 已实现 MP157 到 F4 的二进制协议解析和业务命令分发，可以在现有命令表中扩展补光准备命令。
- **网表检测结果**：工程内未找到 `.net`、`.NET`、`.NetList`、`.kicad_net`、`.asc`、`.cir`、`.spice` 等网表文件；本轮需降级使用 CubeMX `.ioc`、现有源码占用和 STM32F407 官方复用功能表进行引脚冲突分析。
- **工作区保护**：`User/App/robot_arm_service.c`、`User/App/robot_arm_service.h`、`User/App/uart_command.c` 已有未提交修改，本任务不得覆盖或回退这些改动。
- **待确认**：舵机 PWM 目标引脚、补光灯独立 GPIO 接法、270 度舵机的最小/最大有效脉宽仍需完成证据核对和用户确认。
- **定时器占用**：当前 CubeMX 工程只使用 `TIM6` 作为 HAL 系统时基，没有配置任何 PWM 定时器；`TIM4` 在 `.ioc`、`Core/` 和 `User/` 中均未被占用。
- **PWM 候选**：推荐 `PB6 -> AF2/TIM4_CH1`。ST 官方 `STM32F407ZG` 数据手册 `DM00037051` 的 Alternate function mapping 明确列出 `PB6/AF2 = TIM4_CH1`；当前工程也未占用 PB6。
- **补光控制决策**：用户确认只使用 `PB6/TIM4_CH1` 控制舵机，不增加独立灯控 GPIO。舵机从 `0°` 转到 `270°` 后停止 PWM并通过机械动作打开补光灯；模型检测结束后由 MP157 下发复位命令，F4 再输出 PWM让舵机从 `270°` 返回 `0°`，停止 PWM并通过机械动作熄灭补光灯。
- **PWM 时钟条件**：工程 `.ioc` 记录 APB1 定时器时钟为 `50 MHz`。若 TIM4 预分频设为 `49`，可得到 `1 MHz` 计数频率；周期设为 `19999` 可输出标准 `50 Hz/20 ms` 舵机周期。
- **MP157 插入点**：`20_uvc_camera/qt_camera_display/qml/Main.qml` 原先由 `autoVisionStartDetectDelay()` 直接启动检测延时；现已改为先发补光命令，收到 F4 ACK 并继续等真实 `EVENT_REPORT 0x16` 后，再启动固定 `5000 ms` 非阻塞定时器。
- **实现结论**：MP157 不能只等 0x23 ACK；C++ 必须继续等相同 `cycle_id/related_seq/action` 的 `EVENT_REPORT 0x16`，QML 才启动 5000 ms Timer。检测完成后也必须等关灯 0x16，才能清除自动检测标志并回升 Z 轴。
- **停止顺序结论**：F4 的 `STOP_CYCLE` 会清空 `active_cycle_id`，因此暂停/停止收口必须按“ACTUATOR_STOP_NOW -> FILL_LIGHT_CONTROL action=0 -> PAUSE/STOP_CYCLE”排序，否则关灯会因 cycle mismatch 被拒绝。
- **PWM 公式结论**：位置舵机使用绝对目标脉宽，不是累计脉冲步数；默认公式为 `500 + angle * 2000 / 270 us`，0/135/270 度分别得到 500/1500/2500 us。
- **最终跨层审查**：F4 的补光完成事件固定使用 `source=7(FILL_LIGHT)`；MP157 除了 `cycle_id/event_code/related_seq/action/angle` 外，还必须严格匹配该来源字段，避免其它服务的 `EVENT_REPORT` 误推进补光状态机。
- **既有 USART3 改动审查**：机械臂服务当前已经按 9600 波特率改用 `HAL_UART_Receive_IT` 单字节中断接收，但 `.ioc` 和模块文档仍保留 115200/DMA 描述；本轮同步修正为 9600，并把 DMA1 Stream1 标记为 CubeMX 保留但运行时未启用，防止重新生成工程后配置回退。

## 2026-04-17 UART DMA + IDLE 接收改造

- 当前项目平台为 STM32F407 + HAL + CMSIS-RTOS2/FreeRTOS。
- `USART1` 已在 `Core/Src/usart.c` 中配置为 `115200 8N1`，并已打开 `DMA2_Stream2` 作为 RX DMA。
- `USART1_IRQn` 和 `DMA2_Stream2_IRQn` 已在生成代码中启用，`stm32f4xx_it.c` 中也已经正确转发给 HAL。
- 当前 HAL 版本在 `Drivers/STM32F4xx_HAL_Driver/Inc/stm32f4xx_hal_uart.h` 和 `Src/stm32f4xx_hal_uart.c` 中已提供：
  - `HAL_UARTEx_ReceiveToIdle_DMA()`
  - `HAL_UARTEx_RxEventCallback()`
- 因此本轮不需要手写寄存器级 IDLE 检测逻辑，直接采用 HAL 官方的 DMA + 空闲中断接收路径最稳妥。
- 用户需求已从“周期主动发送重量”收敛为“电脑发送查询命令时才返回当前重量”。
- 本轮协议计划采用最小文本命令集：`GET` 查询当前重量；未知命令返回错误提示。
